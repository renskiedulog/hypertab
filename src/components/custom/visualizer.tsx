import { useEffect } from "react";

export default function Visualizer() {
  // useEffect(() => {
  //   chrome.runtime.onMessage.addListener((msg: any) => {
  //     console.log("Newtab received:", msg);
  //   });
  // }, []);

  return <h1>Hello Newtab</h1>;
}
